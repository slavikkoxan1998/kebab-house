services:
  kebab-assistant:
    build:
      context: ..
      dockerfile: assistant-server/Dockerfile
    container_name: kebab_house_assistant
    restart: unless-stopped
    ports:
      - "172.17.0.1:8790:8790"
